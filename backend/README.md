# Backend API

Node.js + Express backend with Prisma ORM and PostgreSQL.

## Setup

1. Install dependencies: `npm install`
2. Configure `.env` file (copy from `.env.example`)
3. Run migrations: `npm run prisma:migrate`
4. Start server: `npm run dev`

## Scripts

- `npm run dev` - Start development server
- `npm run scrape` - Scrape articles from beyondchats.com
- `npm run enhance` - Enhance articles with LLM
- `npm run prisma:studio` - Open Prisma Studio

## API Endpoints

See main README.md for full API documentation.

