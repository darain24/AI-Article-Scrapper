{
  "name": "beyondchats-assignment",
  "version": "1.0.0",
  "description": "BeyondChats Article Management System",
  "private": true,
  "scripts": {
    "build:vercel": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}

